_schema-version: 3.3.0
ID: task4-discovery
version: 1.0.0
description: CAP discovery ingestion service

modules:
  - name: task4-discovery-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      memory: 256M
      disk-quota: 512M
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    requires:
      - name: task4-discovery-auth
      - name: task4-discovery-db

  - name: task4-discovery-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
      no-route: true
      no-start: true
      memory: 256M
      disk-quota: 512M
    requires:
      - name: task4-discovery-db

resources:
  - name: task4-discovery-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: xs-security.json

  - name: task4-discovery-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
